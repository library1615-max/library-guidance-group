(()=>{'use strict';const KEY='libraryGuidanceCMSv2';const defaults={news:[],events:[],resources:[]};const read=()=>{try{return {...defaults,...JSON.parse(localStorage.getItem(KEY)||'{}')}}catch{return defaults}};const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));const st=read();const page=document.body.dataset.page;const grid=document.getElementById('contentGrid');const search=document.getElementById('pageSearch');const filter=document.getElementById('pageFilter');function draw(){if(!grid)return;const q=(search?.value||'').trim().toLowerCase(),f=filter?.value||'all';let items=[];if(page==='news')items=(st.news||[]).filter(x=>!x.draft).filter(x=>(f==='all'||x.cat===f)&&`${x.date} ${x.cat} ${x.title} ${x.desc||''}`.toLowerCase().includes(q)).sort((a,b)=>String(b.date).localeCompare(String(a.date))).map(x=>`<a class="content-card" href="${esc(x.url||'#')}" ${String(x.url||'').startsWith('http')?'target="_blank" rel="noopener"':''}><span class="meta">${esc(x.date||'')} ・ ${esc(x.cat||'公告')}</span><h2>${esc(x.title||'未命名公告')}</h2><p>${esc(x.desc||'')}</p><span class="more">查看內容 →</span></a>`);if(page==='events')items=(st.events||[]).filter(x=>(f==='all'||x.region===f)&&`${x.date} ${x.region} ${x.title} ${x.place||''}`.toLowerCase().includes(q)).sort((a,b)=>String(a.date).localeCompare(String(b.date))).map(x=>`<a class="content-card" href="${esc(x.url||'#')}" ${String(x.url||'').startsWith('http')?'target="_blank" rel="noopener"':''}><div class="event-image" style="background-image:url('${esc(x.image||'images/event-central-workshop.png')}')"></div><span class="meta">${esc(x.date||'')} ・ ${esc(x.region||'活動')}</span><h2>${esc(x.title||'未命名活動')}</h2><p>${esc(x.place||'')}</p><span class="more">活動資訊 →</span></a>`);if(page==='resources')items=(st.resources||[]).filter(x=>(f==='all'||x.category===f)&&`${x.category||''} ${x.title} ${x.desc||''}`.toLowerCase().includes(q)).map(x=>`<a class="content-card" href="${esc(x.url||'#')}" ${String(x.url||'').startsWith('http')?'target="_blank" rel="noopener"':''}><div class="resource-icon">${esc(x.icon||'冊')}</div><span class="meta">${esc(x.category||'資源')}</span><h2>${esc(x.title||'未命名資源')}</h2><p>${esc(x.desc||'')}</p><span class="more">開啟資源 →</span></a>`);grid.innerHTML=items.length?items.join(''):'<div class="empty">目前沒有符合條件的內容。</div>'}search?.addEventListener('input',draw);filter?.addEventListener('change',draw);draw();})();